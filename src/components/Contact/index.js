import './index.scss'
import { Link } from 'react-router-dom';

const Contact = () => {
    return  (
        <div className="contact-page">
            <Link to="/">Home</Link>
        </div>
    );
}

export default Contact